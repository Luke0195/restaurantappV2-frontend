version: '3.9'

services:
  web:
    restart: always
    container_name: waiterappweb
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '5173:5173'
    volumes:
      - /app/node_modules
      - .:/app
